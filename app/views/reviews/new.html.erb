<div class="container">
  <div class="row">
    <div class="col-6 offset-3">
    <h1>Laisser un commentaire pour le restaurant <%= @restaurant.name.upcase %></h1>
      <%= simple_form_for([@restaurant, @review]) do |f| %>
      <%= f.input :content %>
      <%= f.submit "valider le commentaire" %>
      <% end %>

    </div>
  </div>
</div>
